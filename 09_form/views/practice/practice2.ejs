<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습문제 2</title>
  </head>
  <body>
    <h1>POST로 정보 받기</h1>

    <form action="/practice1_get" method="get">

        <label> 이름 : <input type="text" name="name" id="name" /></label>

        <fieldset>
          <legend>성별</legend>
          <label><input type="radio" name="sex" id="male" value="남자"/>남자</label>
          <label><input type="radio" name="sex" id="female" value="여자"/>여자</label>
        </fieldset>

        <fieldset>
          <legend>생년월일</legend>
          <label><select name="year" id="year">
            <% for(let i=1990; i<=2024; i++){ %>
              <option value="<%=i %>"><%=i%></option>
            <% } %>
          </select>년</label>
          <label><select name="month" id="month">
            <% for(let i=1; i<=12; i++){ %>
              <option value="<%=i %>"><%=i%></option>
            <% } %>
          </select>월</label>
          <label><select name="day" id="day">
            <% for(let i=1; i<=31; i++){ %>
              <option value="<%=i %>"><%=i%></option>
            <% } %>
          </select>일</label>
          </select>
        </fieldset>

        <fieldset>
          <legend>관심사</legend>
          <label><input type="checkbox" name="hobby" id="travel" value="여행">여행</label>
          <label><input type="checkbox" name="hobby" id="fassion" value="패션">패션</label>
          <label><input type="checkbox" name="hobby" id="food" value="음식">음식</label>
        </fieldset>

        <fieldset>
            <legend>선택</legend>
            <label>이메일 주소 : <input type="email" name="email" id="email"></label>
            <br>
            <br>
            <label>좋아하는 색깔 : <input type="color" name="color" id="color"></label>
          </fieldset>

        <br>
        
        <button>회원가입</button>
      </form>
  </body>
</html>
